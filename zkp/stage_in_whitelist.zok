// ZoKrates program: prove stage is in an authorized set (example: 0..5).
// Witness: stage_code (private).
// No public inputs (privacy-preserving, but not bound to a specific event; binding is future work).
def main(private field stage) -> field {
    // Authorized set: {0,1,2,3,4,5}
    // Enforce membership by polynomial constraint: Π(stage - a_i) == 0
    field p = stage * (stage - 1) * (stage - 2) * (stage - 3) * (stage - 4) * (stage - 5);
    assert(p == 0);
    return 1;
}
